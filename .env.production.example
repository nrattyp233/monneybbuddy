# Production env example (do NOT commit real secrets)# Production env example (do NOT commit real secrets)

# Copy to .env.production locally; set real values in your host/CI environment.# Copy to .env.production locally; set real values in your host/CI environment.



VITE_SUPABASE_URL=https://YOUR-PROJECT.ref.supabase.coVITE_SUPABASE_URL=https://YOUR-PROJECT.ref.supabase.co

VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEYVITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY

VITE_USE_MOCKS=falseVITE_USE_MOCKS=false

# Optional: Frontend-only key used for non-sensitive features# Optional: Frontend-only key used for non-sensitive features

VITE_GEMINI_API_KEY=VITE_GEMINI_API_KEY=

