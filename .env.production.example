# MoneyBuddy Production Environment Configuration
# Copy this file to .env.production and fill in your actual values

# ==========================================
# SUPABASE CONFIGURATION (Required)
# ==========================================
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# For Edge Functions (Server-side only)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# ==========================================
# PLAID CONFIGURATION (Required)
# ==========================================
PLAID_CLIENT_ID=your-plaid-client-id
PLAID_SECRET=your-plaid-secret-key
PLAID_ENV=production
# Valid values: sandbox, development, production

# ==========================================
# PAYPAL CONFIGURATION (Required)
# ==========================================
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_CLIENT_SECRET=your-paypal-client-secret
PAYPAL_API_URL=https://api-m.paypal.com
# Use https://api-m.paypal.com for production
# Use https://api.sandbox.paypal.com for sandbox

# ==========================================
# SECURITY CONFIGURATION (Required)
# ==========================================
ALLOWED_ORIGIN=https://your-domain.com
# Your frontend domain - do NOT use '*' in production

# ==========================================
# APPLICATION CONFIGURATION
# ==========================================
NODE_ENV=production
DENO_ENV=production
APP_VERSION=1.0.0
VITE_USE_MOCKS=false

# Optional: Frontend-only key used for non-sensitive features
VITE_GEMINI_API_KEY=

# ==========================================
# DEPLOYMENT VERIFICATION CHECKLIST
# ==========================================
# ✅ All environment variables are set
# ✅ Database migrations are applied
# ✅ Edge Functions are deployed
# ✅ CORS configuration is secure
# ✅ SSL certificates are configured
# ✅ PayPal webhook endpoints are configured
# ✅ Plaid webhooks are configured (optional)
# ✅ Monitoring and logging are set up
# ✅ Backup strategies are in place

# ==========================================
# SECURITY NOTES
# ==========================================
# 1. Never commit actual secrets to version control
# 2. Use environment-specific values for each deployment
# 3. Regularly rotate API keys and secrets
# 4. Monitor logs for suspicious activity
# 5. Keep all dependencies updated
# 6. Use HTTPS only in production
# 7. Implement proper error handling
# 8. Regular security audits recommended

# ==========================================
# DEPLOYMENT COMMANDS
# ==========================================
# Deploy Edge Functions:
# supabase functions deploy create-link-token
# supabase functions deploy exchange-public-token
# supabase functions deploy refresh-account-balances
# supabase functions deploy create-paypal-order
# supabase functions deploy capture-paypal-order

# Apply Database Migrations:
# supabase db push

# Build and Deploy Frontend:
# npm run build
# Upload dist/ to your hosting provider

